<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="main.member">

	<insert id="signup" parameterType="member">
		insert into member values
		(#{id}, #{pwd}, #{name}, #{phone}, #{email}, '1', '1')
	</insert>

	<insert id="kakao" parameterType="member">
		insert into member values
		(#{id}, null, #{name}, null, #{email}, '2', '1')
	</insert>
	
	<select id="login" parameterType="Map" resultType="member">
		select * from member where id = #{id} and pwd = #{pwd}
	</select>

	<select id="idcheck" parameterType="String" resultType="member">
		select * from member where id = #{id}
	</select>

	<update id="update" parameterType="member">
		update member set
		pwd=#{pwd}, phone=#{phone}, email=#{email}, alarm=#{alarm} where
		id=#{id}
	</update>

	<update id="withdraw" parameterType="member">
		update member set
		state='0' where
		id=#{id}
	</update>
	
</mapper>